/* eslint-env mocha */
const assert = require('assert');

const LevelUtil = require('../lib/LevelUtil');

describe('Library: LevelUtil', function() {
  it('exists', function() {
    assert.ok(!!LevelUtil);
  });
});